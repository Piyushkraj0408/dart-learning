void main() {
  List<String> names = ["Ramesh", "Mukesh", "dhruv"];
  // var count = 0;
  // or (int s = 0; s < names.length; s++) {
  //   if(count)
  //   count++;
  // }f
  // print(count);
  for (String s in names) {
    print(s);
  }
}
